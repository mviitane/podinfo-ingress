apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: podinfo-ingress
  namespace: default
spec:
  ingressClassName: nginx
  rules:
  - host: podinfo.13.51.167.148.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: podinfo
            port: 
              number: 9898
